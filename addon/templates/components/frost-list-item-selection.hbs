{{#if singleSelection}}
  {{frost-radio-button 
    checked=isSelected
    hook=(concat hookPrefix '-radio-button')
    size=size
    value=(concat index '')
  }}
{{else}}
  {{frost-checkbox
    checked=isSelected
    hook=(concat hookPrefix '-checkbox')
    size=size
  }}
{{/if}}
